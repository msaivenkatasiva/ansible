- name: install nginx and run
  hosts: web
  tasks: 
    - name: install nginx package
      ansible.builtin.package:
        name: nginx
        state: present
    - name: run nginx
      state: started
