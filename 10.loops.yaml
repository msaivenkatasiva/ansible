- name: printing loops
  hosts: localhost
  tasks:
  - name: printing the given loops
    ansible.builtin.package:
      msg: "hello {{item}}"
    loop:
    - siva
    - sankar
    - deva
    - mahadev